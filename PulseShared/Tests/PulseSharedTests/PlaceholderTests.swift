import XCTest
@testable import PulseShared

final class PlaceholderTests: XCTestCase {
    func testPlaceholderExists() {
        // Trivial passing test — deleted in Task 1
        XCTAssertTrue(true)
    }
}
